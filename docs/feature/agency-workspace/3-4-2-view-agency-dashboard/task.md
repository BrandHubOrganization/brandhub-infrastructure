# Task — View Agency Dashboard (FR 3.4.2)

> Trạng thái: **Draft — chưa bắt đầu, chờ thiết kế kỹ thuật.**

- [ ] Chưa có task cụ thể — chờ BA/BE lead chốt API Contract (`GET /agencies/{agencyId}/dashboard`) và nguồn dữ liệu `recentActivity` ở [plan.md](plan.md) trước khi bóc tách checklist BE/FE.
